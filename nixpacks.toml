[phases.setup]
nixPkgs = ["chromium", "glib", "nss", "nspr", "atk", "cups", "libdrm", "mesa", "libxkbcommon", "pango", "cairo", "alsa-lib", "expat"]

[variables]
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD = "true"
PUPPETEER_EXECUTABLE_PATH = "/run/current-system/sw/bin/chromium"
